---
import { renderLegacyContent } from '../lib/renderLegacyContent';

const { content = '', className = '' } = Astro.props;
---

<div class:list={['legacy-content', className]} set:html={renderLegacyContent(content)} />
